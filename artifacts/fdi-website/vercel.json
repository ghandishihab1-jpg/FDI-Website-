{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter @workspace/fdi-website run build",
  "outputDirectory": "artifacts/fdi-website/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
