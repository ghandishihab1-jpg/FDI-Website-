kind = "web"
previewPath = "/"
title = "FDI – Future Doctor Initiative"
version = "1.0.0"
id = "artifacts/fdi-website"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19294

[services.development]
run = "pnpm --filter @workspace/fdi-website run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fdi-website", "run", "build" ]
publicDir = "artifacts/fdi-website/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19294"
BASE_PATH = "/"
